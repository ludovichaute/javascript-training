//Exercice 4

var tab = [-2, 1, 4];

function additionne(x) {
  var add = x + 2;

  return add;

}
function afficher(){
    var a = tab[0];
    alert("l'addition de "+a+" est: "+additionne(a));
    var a = tab[2];
    alert("l'addition de "+a+" est: "+additionne(a));

  }
